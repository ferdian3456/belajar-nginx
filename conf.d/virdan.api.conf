# server {
#     listen 80;
#     server_name api.virdan.com;
#     return 301 https://$host$request_uri;
# }

# server {
#     listen 443 ssl;
#     server_name api.virdan.com;

#     ssl_certificate     /etc/letsencrypt/live/api.virdan.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.virdan.com/privkey.pem;

#     access_log /var/log/nginx/virdan-api.access.log main;
#     error_log  /var/log/nginx/virdan-api.error.log warn;

#     location /api/auth/ {
#         limit_req zone=auth_limit burst=5 nodelay;
#         limit_req_status 429;

#         proxy_pass http://host.docker.internal:8081;
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 10s;
#         proxy_read_timeout    60s;
#     }

#     location / {
#         limit_req zone=api_limit burst=50 nodelay;
#         limit_req_status 429;

#         proxy_pass http://host.docker.internal:8081;
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 10s;
#         proxy_read_timeout    60s;
#     }
# }

## local no https
server {
    listen 80;
    server_name localhost;

    access_log /var/log/nginx/virdan-api.access.log main;
    error_log  /var/log/nginx/virdan-api.error.log warn;

    location /api/auth/ {
        limit_req zone=auth_limit burst=5 nodelay;
        limit_req_status 429;

        proxy_pass http://host.docker.internal:8081;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_read_timeout    60s;
    }

    location / {
        limit_req zone=api_limit burst=50 nodelay;
        limit_req_status 429;

        proxy_pass http://host.docker.internal:8081;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_read_timeout    60s;
    }
}